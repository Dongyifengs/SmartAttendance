# 项目结构说明 (Project Structure Guide)

## 目录结构

```
SmartAttendance/
├── public/                      # 静态资源
│   ├── vite.svg                # Vite 图标
│   └── zhong.svg               # 钟图标
├── src/                        # 源代码目录
│   ├── api/                    # API 接口层
│   │   ├── anlaxy/            # 智慧考勤 API
│   │   │   ├── crypto/        # 加密相关
│   │   │   ├── endpoint/      # API 端点定义
│   │   │   ├── type/          # TypeScript 类型定义
│   │   │   ├── utils/         # 工具函数
│   │   │   └── index.ts       # 主接口导出
│   │   ├── moyi/              # MOYI 日志 API
│   │   │   └── index.ts
│   │   └── oc/                # 一卡通 API
│   │       ├── endpoint/      # API 端点定义
│   │       ├── type/          # TypeScript 类型定义
│   │       └── index.ts       # 主接口导出
│   ├── components/            # 公共组件
│   │   ├── ClassCard.vue      # 课程卡片组件
│   │   ├── ClassContainer.vue # 课程容器组件
│   │   └── IconifyText.vue    # 图标文本组件
│   ├── composables/           # 组合式函数
│   │   ├── useAirConditioning.ts  # 空调管理
│   │   ├── useApiCall.ts          # API 调用封装
│   │   ├── useAttendance.ts       # 签到签退
│   │   ├── useLoading.ts          # 加载状态管理 ⭐新增
│   │   ├── useOneCard.ts          # 一卡通操作
│   │   ├── usePaymentQR.ts        # 支付二维码
│   │   ├── useUserInfo.ts         # 用户信息管理
│   │   ├── index.ts               # 导出入口
│   │   └── README.md              # 使用文档
│   ├── router/                # 路由配置
│   │   └── index.ts           # 路由定义
│   ├── styles/                # 全局样式 ⭐新增
│   │   ├── variables.css      # CSS 变量定义
│   │   ├── common.css         # 通用样式
│   │   └── README.md          # 样式文档
│   ├── utils/                 # 工具函数 ⭐新增
│   │   ├── constants.ts       # 应用常量
│   │   ├── dateTime.ts        # 日期时间工具
│   │   ├── errorHandler.ts   # 错误处理
│   │   ├── storage.ts         # 本地存储工具
│   │   ├── index.ts           # 导出入口
│   │   └── README.md          # 工具文档
│   ├── view/                  # 页面视图
│   │   ├── anlaxy-dev/        # 开发者工具页面
│   │   │   ├── components/    # 子组件
│   │   │   ├── style/         # 样式文件
│   │   │   ├── AttendanceDev.vue  # 考勤查询
│   │   │   ├── ClassListDev.vue   # 课程列表
│   │   │   ├── LeaveDev.vue       # 请假管理
│   │   │   └── types.d.ts         # 类型定义
│   │   ├── home/              # 主页
│   │   │   ├── style/         # 样式文件
│   │   │   └── PageHome.vue
│   │   ├── login/             # 登录页面
│   │   │   ├── style/         # 样式文件
│   │   │   └── PageLogin.vue
│   │   └── newHome/           # 新主页
│   │       └── DevHome.vue
│   ├── App.vue                # 根组件
│   ├── App.css                # 全局应用样式
│   ├── main.ts                # 应用入口
│   └── vite-env.d.ts          # Vite 环境类型
├── .env.production            # 生产环境变量
├── .eslintrc.js               # ESLint 配置（旧）
├── .gitignore                 # Git 忽略文件
├── .prettierignore            # Prettier 忽略文件
├── .prettierrc                # Prettier 配置
├── eslint.config.js           # ESLint 配置（新）
├── index.html                 # HTML 入口
├── package.json               # 项目配置
├── tsconfig.json              # TypeScript 配置
├── tsconfig.app.json          # 应用 TypeScript 配置
├── tsconfig.node.json         # Node TypeScript 配置
├── vite.config.ts             # Vite 配置
├── LICENSE                    # 开源协议（AGPL-3.0）
└── README.md                  # 项目说明文档
```

## 核心模块说明

### 🛠️ Utils（工具函数）

**位置**: `src/utils/`

**说明**: 包含项目中使用的所有通用工具函数

**主要模块**:
- `storage.ts` - 本地存储工具，统一管理 localStorage
- `constants.ts` - 应用常量定义
- `errorHandler.ts` - 错误处理和消息显示
- `dateTime.ts` - 日期时间操作

**使用示例**:
```typescript
import { getStorageItem, showError, formatDate } from '@/utils';
```

### 🎨 Styles（样式系统）

**位置**: `src/styles/`

**说明**: 全局样式和CSS变量定义

**主要文件**:
- `variables.css` - CSS变量（颜色、间距、圆角等）
- `common.css` - 通用样式类和动画

**特点**:
- 100+ CSS变量定义
- 响应式设计支持
- 工具类和动画定义

### 🔧 Composables（组合式函数）

**位置**: `src/composables/`

**说明**: Vue 3 组合式函数，封装可复用的业务逻辑

**主要模块**:
- `useUserInfo` - 用户信息管理
- `useLoading` - 加载状态管理
- `useApiCall` - API 调用封装
- `useAttendance` - 签到签退
- `useOneCard` - 一卡通操作
- `usePaymentQR` - 支付二维码
- `useAirConditioning` - 空调管理

### 🌐 API（接口层）

**位置**: `src/api/`

**说明**: 与后端 API 交互的接口层

**模块划分**:
- `anlaxy/` - 智慧考勤 API
- `oc/` - 一卡通 API
- `moyi/` - MOYI 日志 API

### 🧩 Components（组件）

**位置**: `src/components/`

**说明**: 可复用的 Vue 组件

**主要组件**:
- `ClassCard.vue` - 课程卡片
- `ClassContainer.vue` - 课程容器
- `IconifyText.vue` - 图标文本

### 📄 Views（页面视图）

**位置**: `src/view/`

**说明**: 应用的页面组件

**页面结构**:
- `login/` - 登录页面
- `home/` - 主页
- `newHome/` - 新主页
- `anlaxy-dev/` - 开发者工具

## 命名规范

### 文件命名

- **组件**: PascalCase（例如：`ClassCard.vue`）
- **工具函数**: camelCase（例如：`storage.ts`）
- **样式文件**: kebab-case（例如：`common.css`）
- **类型定义**: camelCase + `.d.ts`（例如：`types.d.ts`）

### 代码命名

- **变量和函数**: camelCase
- **常量**: UPPER_SNAKE_CASE
- **类和接口**: PascalCase
- **CSS类名**: kebab-case

### 目录命名

- **小写 + 连字符**: kebab-case（例如：`anlaxy-dev/`）
- **单数形式**: 除非表示集合（例如：`utils/`, `styles/`）

## 最佳实践

### 1. 导入规范

```typescript
// 优先使用别名导入
import { useUserInfo } from '@/composables';
import { getStorageItem } from '@/utils';

// 避免相对路径
// import { useUserInfo } from '../../composables/useUserInfo';
```

### 2. 组件组织

```vue
<template>
  <!-- HTML 结构 -->
</template>

<script lang="ts" setup>
  // 导入
  // 类型定义
  // 响应式数据
  // 计算属性
  // 方法
  // 生命周期钩子
</script>

<style scoped>
  /* 使用 CSS 变量 */
  /* 使用 scoped 避免样式污染 */
</style>
```

### 3. 样式使用

```vue
<style scoped>
/* 使用 CSS 变量 */
.my-component {
  color: var(--color-primary);
  padding: var(--spacing-md);
  border-radius: var(--radius-md);
}

/* 使用工具类 */
/* @import '@/styles/common.css'; */
</style>
```

### 4. 类型定义

```typescript
// 在 .d.ts 文件中定义类型
export interface UserInfo {
  id: string;
  name: string;
}

// 在 .ts 文件中使用
import type { UserInfo } from './types.d';
```

## 开发流程

### 1. 添加新功能

1. 在 `src/utils/` 中添加通用工具函数
2. 在 `src/composables/` 中添加业务逻辑
3. 在 `src/components/` 中创建组件
4. 在 `src/view/` 中创建页面
5. 在 `src/router/` 中添加路由

### 2. 修改样式

1. 优先修改 `src/styles/variables.css` 中的变量
2. 在组件中使用 CSS 变量
3. 必要时添加新的工具类到 `common.css`

### 3. 添加 API

1. 在 `src/api/` 相应目录中添加端点
2. 在 `type/` 目录中定义类型
3. 在 `index.ts` 中导出接口

## 升级说明

### ⭐ 新增内容

1. **工具函数库** (`src/utils/`)
   - 统一的存储管理
   - 错误处理机制
   - 日期时间工具
   - 应用常量定义

2. **样式系统** (`src/styles/`)
   - CSS 变量系统
   - 通用样式类
   - 动画定义

3. **组合式函数**
   - useLoading - 加载状态管理

4. **文档完善**
   - 每个模块都有 README
   - 包含使用示例和最佳实践

### 🔄 改进内容

1. **类型安全**: 更完善的 TypeScript 类型定义
2. **代码复用**: 通过工具函数和 composables 提高复用率
3. **一致性**: 统一的命名规范和代码风格
4. **可维护性**: 清晰的代码结构和文档

## 技术栈

- **框架**: Vue 3.5.18
- **语言**: TypeScript 5.8.3
- **构建工具**: Vite 7.1.11
- **UI 组件库**: Element Plus 2.11.3
- **路由**: Vue Router 4.5.1
- **HTTP 客户端**: Axios 1.11.0
- **日期库**: Day.js 1.11.18

## 参考资料

- [Vue 3 文档](https://vuejs.org/)
- [TypeScript 文档](https://www.typescriptlang.org/)
- [Element Plus 文档](https://element-plus.org/)
- [Vite 文档](https://vitejs.dev/)
